{
  "name": "@aegis/scanner",
  "version": "0.1.0",
  "description": "Pre-trade safety scanner — deterministic GO / CAUTION / NO verdict from an on-chain + market snapshot. Deterministic check battery, part of the Aegis token-scanning skill.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures": "./src/fixtures.ts"
  },
  "scripts": {
    "test": "node --import tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
